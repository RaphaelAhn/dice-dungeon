# AI 활용 기술 문서 — 「주사위 던전」

> NAN 2026 Game × AI 해커톤 사전 과제
> 저장소 https://github.com/RaphaelAhn/dice-dungeon
> 플레이 https://raphaelahn.github.io/dice-dungeon/

---

## 1. 요약

이 게임에서 AI는 두 층위에서 쓰였다.

| 층위 | 무엇을 했나 | 증거 |
|---|---|---|
| **개발 과정** | 코드 전량 작성, 밸런스 수치 산출, 설계 결함 발견 | 커밋 기록, `tools/sim.ts` |
| **게임 내부** | 보상 3택 구성, 스킬 계열 편향, 전직 판정 | `src/core/reward.ts`, `job.ts` |

**무게중심은 개발 과정 쪽에 있다.** 게임 안에 LLM을 호출하는 부분은 없다. 그렇게 만든 이유는 §2.1에 적었다.

가장 내세우고 싶은 것은 §4다. **AI가 사람이 놓친 설계 결함을 세 번 잡아냈고, 그때마다 규칙이 바뀌었다.** 그 과정이 커밋 기록에 그대로 남아 있다.

---

## 2. 게임 내부의 AI

### 2.1 LLM을 쓰지 않은 이유

처음 기획은 층 사이에 "AI 던전 마스터 코멘트"를 LLM으로 붙이는 것이었다. 두 가지 이유로 접었다.

**규정.** 사전 과제는 *"별도 유료 라이선스 없이 심사자가 실행할 수 있어야 한다"* 고 요구한다. 브라우저에서 도는 게임이 LLM을 호출하려면 API 키가 클라이언트 번들에 들어가고, 공개 저장소에 그 키가 그대로 노출된다. 키를 빼면 심사자 환경에서 게임이 동작하지 않는다. 어느 쪽도 조건을 만족하지 못한다.

**판정 가능성.** 매 실행 결과가 달라지면 밸런스를 측정할 수 없다. §3.2의 시뮬레이션은 같은 시드에 같은 결과가 나오는 것을 전제로 한다.

그래서 게임 내부의 판단은 **결정론적 규칙**으로 구현하고, AI는 그 규칙을 설계하고 검증하는 쪽에 썼다. 외부 의존이 0이라 심사자는 링크만 누르면 된다.

### 2.2 보상 3택 구성 — `src/core/reward.ts`

스테이지 보상은 세 장을 제시하고 하나만 고르게 한다. 세 장은 무작위가 아니라 **서로 대체 불가능한 세 슬롯**에서 한 장씩 뽑는다.

| 슬롯 | 성격 | 고르면 포기하는 것 |
|---|---|---|
| A | 스킬 (빌드) | 능력치 성장 |
| B | 능력치 / 회복 (기반) | 빌드 진행 |
| C | 도박 (큰 상승 + 명시된 대가) | 안전 |

핵심은 **"제일 센 것"이라는 답이 없게 만드는 것**이다. 세 장이 같은 축에 있으면 비교가 가능해지고, 비교가 가능하면 고민이 사라진다.

카드는 두 단계로 만든다.

1. **티어 추첨** — 브론즈 40% / 실버 35% / 골드 20% / 플래티넘 5%. 행운 스탯이 하위 티어 확률을 상위로 옮긴다. 티어는 **세 장 각각 따로 굴린다** — 한 벌을 묶어 굴리면 "브론즈 지점"이 생겨 편차가 커진다.
2. **상태 반응** — 체력이 절반 아래로 떨어지면 B 슬롯에 회복 카드가 확률적으로 등장한다. 플레이어 상태를 보고 선택지 구성이 달라진다.

### 2.3 능력치 → 스킬 계열 편향

주사위로 받은 능력치가 **보상에 등장하는 스킬 계열의 확률을 바꾼다.**

```ts
// src/core/skill.ts
export function affinity(stats: Stats, line: SkillLine): number {
  switch (line) {
    case 'sword': return stats.atk
    case 'fire': case 'ice': return stats.mag
    case 'holy': return stats.luk
    case 'dark': return stats.spd
  }
}
```

마법력이 높게 시작하면 화염·얼음 스킬이 더 자주 나온다. **강제가 아니라 편향이다** — 마법형으로 시작해도 검술 스킬은 나오고, 그걸 골라 검사로 가도 된다.

이 고리가 없으면 "주사위가 직업 선택에 유리하게 작용한다"는 설계가 성립하지 않는다. 실제로 개발 중간에 이 연결이 빠져 있다는 것을 발견하고 추가했다.

### 2.4 전직 판정 — `src/core/job.ts`

1-8에 진입하면 무조건 전직한다. **어떤 직업이 되는지는 그때까지의 선택이 정한다.**

- 주계열 = 가장 많이 보유한 계열
- 부계열 = 두 번째로 많은 계열
- **보유 수가 같으면 능력치 친화도가 높은 쪽이 이긴다** — 주사위가 여기서 한 번 더 방향을 잡는다
- 스킬이 하나도 없어도 친화도만으로 직업이 정해진다

직업 25종을 하나씩 만들지 않는다. **주계열 5행 + 부계열 5행 = 10행의 데이터**와 판정 함수 1개로 25종이 자동 생성된다. 계열을 하나 추가하면 36종으로 늘어난다.

전직을 막지 않는 대신 **계열 집중도가 보너스 크기를 정한다.**

| 등급 | 조건 | 보너스 |
|---|---|---|
| 완성 | 3+0 순수, 또는 주계열 2 + 부계열 | 전량 + 부계열 특성 |
| 미완 | 주계열 2, 부계열 없음 | 70% + 특성 |
| 빈약 | 주계열 1 이하 | 40%, 특성 없음 |

계열을 흩뿌려도 "전직 실패"가 아니라 "약한 전직"이 된다. 막다른 길을 만들지 않으면서 집중할 유인은 남긴다.

---

## 3. 개발 과정의 AI

### 3.1 역할 분담

| 사람 (디렉터) | AI |
|---|---|
| 게임 규칙 결정 | 전체 코드 작성 |
| 재미 판정 | 규칙의 결과를 수치로 검증 |
| 우선순위 판단 | 설계 충돌·결함 지적 |
| 최종 채택 | 문서 작성 |

**코드는 전량 AI가 작성했다.** 사람은 규칙을 정하고 방향을 잡았다. 커밋 기록이 그 과정 그대로다 — 규칙이 바뀔 때마다 무엇을 왜 바꿨는지가 커밋 메시지에 남아 있다.

### 3.2 밸런스 시뮬레이터 — `tools/sim.ts`

수치를 감으로 정하지 않았다. **실제 전투·보상·전직 코어를 그대로 불러 쓰는 시뮬레이터**를 만들어 360판을 자동으로 돌렸다.

```bash
npm run sim
```

핵심은 시뮬레이터가 게임 규칙을 **베끼지 않고 import 한다**는 점이다. 수치를 따로 적어 두면 코드를 고칠 때마다 어긋나서 측정값이 거짓말을 한다.

측정 항목:
- 보상 선택 정책 5종 × 주사위 6면 × 60판
- 정책별 클리어율, 평균 소요 턴
- 실패 원인(사망 / 시간 초과)의 스테이지 분포

난수는 시드 기반(mulberry32)이라 **같은 시드면 같은 결과가 나온다.** 수치를 바꾸고 다시 돌리면 그 변화만 관찰된다.

---

## 4. AI가 잡아낸 설계 결함 3건

이 문서에서 가장 중요한 부분이다. 세 건 모두 **사람이 규칙을 정했고, AI가 그 규칙의 결과를 측정해 문제를 드러냈고, 규칙이 바뀌었다.**

### 4.1 회복 시점이 보상 조합 간 난이도를 어긋나게 만들고 있었다

보상 지점은 판마다 `[1-1, 1-3, 1-9]` 또는 `[1-2, 1-4, 1-8]` 중 하나로 추첨된다. 처음에는 **두 조합의 난이도가 같다**고 판단했다. 근거는 두 조합 모두 1-5 중간보스와 1-10 보스 진입 시점의 누적 카드가 6장 / 9장으로 동일하다는 것이었다.

시뮬레이션 결과 사망이 1-7~1-9 구간에 몰렸다.

```
1-5  131판   ██████████████
1-7  201판   █████████████████████
1-8  329판   ███████████████████████████████████
1-9  379판   ████████████████████████████████████████
```

원인은 카드 수가 아니라 **회복 간격**이었다. 당시 체력 회복을 보상 지점에 묶어 뒀는데, 조합 `[1,3,9]`는 4번부터 9번까지 **여섯 스테이지를 회복 없이** 통과해야 했다. 조합 `[2,4,8]`은 최대 네 스테이지였다.

카드 수만 보고 "난이도가 같다"고 한 판단이 틀렸다. 회복을 넣는 순간 전제가 깨진다.

**조치:** 회복을 보상 지점에서 떼어 **스테이지 클리어마다 최대 체력의 25%**로 바꿨다. 조합과 무관해졌다.

### 4.2 계열을 흩뿌리는 쪽이 모으는 쪽보다 유리했다

설계 의도는 "계열을 모아야 강해진다"였다. 측정값은 반대였다.

| 보상 정책 | 클리어율 |
|---|---|
| 집중 (같은 계열 우선) | 50% |
| **분산 (계열 안 보고 스킬)** | **61%** |

원인은 **전직 보너스가 아직 구현되지 않아서**였다. 보너스가 없으면 계열을 흩뿌릴수록 쓸 수 있는 스킬 종류만 늘어난다. 집중할 이유가 게임 안에 존재하지 않았다.

여기서 중요한 판단이 하나 있었다. 이 시점에 적 수치를 조정하고 싶은 유혹이 있었지만, **미완성 시스템에 맞춰 튜닝하는 것이 되므로 보류**했다. 전직 보너스를 먼저 구현하고 다시 측정했다.

| 보상 정책 | 구현 전 | 구현 후 |
|---|---|---|
| 집중 | 50% | **64%** |
| 분산 | 61% | **47%** |

역전이 해소됐다.

### 4.3 주사위가 승패를 확정하고 있었다

기획의 두 축은 이렇다.

1. 주사위는 초반에 큰 영향을 미친다
2. 주사위가 나빠도 선택을 잘하면 이어갈 수 있다

측정해 보니 2번이 성립하지 않았다.

```
변경 전:  눈1 100%  눈2 0%  눈3 100%  눈4 100%  눈5 0%  눈6 0%
```

중간이 없었다. **주사위를 굴리는 순간 승패가 정해졌다.** 빌드와 눈이 맞으면 이기고 어긋나면 지는데, 어긋났을 때 보상으로 만회할 여지가 없었다.

**조치:** 전직 보너스와 보상 티어를 구현하고, 눈 3(마법력)이 마나까지 겸해 혼자 강했던 것을 15→12로 낮추고, 눈 6이 처지던 것을 티어 확정 1→2회로 올렸다.

```
변경 후:  눈1 60%  눈2 58%  눈3 83%  눈4 67%  눈5 55%  눈6 62%
```

**눈은 유리함을 주지만 결과를 정하지 않는다.** 두 축이 동시에 성립하게 됐다.

---

## 5. 주요 지시 사항

사람이 AI에게 준 규칙 중, 설계를 크게 바꾼 것들이다. 각 지시는 커밋 하나 이상으로 이어진다.

| 지시 | 결과 |
|---|---|
| "주사위는 능력치를 설정하는 것" | 축복 6종을 폐기하고 능력치 5종 + 티어 확정으로 재설계 |
| "보상은 3개 중 1개" | 주사위가 선택지 수를 바꾸던 규칙 폐기, `REWARD_CHOICES` 상수로 고정 |
| "보상은 라운드 3개가 끝나면" | 매 스테이지 지급 → 지점 3곳. 단 지점당 3장을 골라 선택 횟수 9회는 유지 |
| "1,3,9 또는 2,4,8 중 랜덤" | 보상 지점 조합 추첨. 순서를 외워 최적해를 굳히는 것을 막는다 |
| "전직은 1-8에서 무조건" | 전직 조건 폐기. 대신 계열 집중도로 보너스를 차등 |
| "턴당 10초, 스테이지 100초, 보스 150초" | 제한 시간 두 겹. 100÷10=10턴이라 시간 제한이 턴 예산을 겸한다 |

AI가 지시를 그대로 받기만 한 것은 아니다. **지시가 다른 규칙과 충돌할 때는 충돌 지점을 먼저 제시했다.** 예를 들어 "보상을 3라운드마다"는 지시는 그대로 따르면 총 보상이 3회가 되어 전직에 필요한 스킬 3개를 채우고 나면 선택의 여지가 사라진다. 이를 지적해 "지점은 줄이고 선택 횟수는 유지"로 정리했다.

---

## 6. 외부 에셋 · 오픈소스

### 6.1 외부 에셋 — 없음

**이미지·사운드·폰트 파일을 하나도 쓰지 않았다.** 캐릭터 스프라이트, 주사위, 적, 게이지, 이펙트가 전부 CSS 도형과 그라디언트로 그려져 있다.

- `src/ui/CharacterSprite.tsx` / `.css` — 캐릭터 실루엣
- `src/ui/Dice.tsx` / `.css` — 3×3 그리드로 그린 주사위 눈
- `src/ui/Battle.css` — 적, 체력 게이지, 상태 표시

따라서 저작권·라이선스 표기 대상이 없다. 폰트도 시스템 폰트만 지정한다.

### 6.2 오픈소스 의존성

| 패키지 | 라이선스 | 용도 |
|---|---|---|
| react / react-dom 18.3 | MIT | UI |
| vite 6 | MIT | 번들러 |
| typescript 5.6 | Apache-2.0 | 타입 검사 |
| @vitejs/plugin-react | MIT | 빌드 |
| esbuild | MIT | 시뮬레이터 실행 |

런타임 의존성은 React뿐이다. 빌드 결과물은 **JS 176.8KB + CSS 17.2KB = 약 194KB (gzip 62KB)** 로, 링크를 누르면 즉시 실행된다.

---

## 7. 재현 방법

```bash
npm install
npm run dev      # 개발 서버
npm run build    # 프로덕션 빌드 (타입 검사 포함)
npm run sim      # 밸런스 시뮬레이션 360판
```

`main` 브랜치에 푸시하면 GitHub Actions가 빌드 후 Pages로 자동 배포한다 (`.github/workflows/deploy.yml`). 빌드 스크립트에 `tsc -b`가 물려 있어 타입 오류가 있으면 배포가 중단된다.

---

## 8. 한계와 남은 과제

정직하게 적는다.

- **게임 내 AI는 규칙 기반이다.** 학습 모델이나 LLM 추론은 들어 있지 않다. §2.1의 이유로 의도적으로 배제했다.
- **밸런스 수치는 시뮬레이션 기준이다.** 시뮬레이터의 전투 판단은 사람보다 단순하므로, 실제 플레이어의 클리어율은 이보다 높을 가능성이 크다.
- **한 턴의 체감 시간은 아직 실측하지 않았다.** 제한 시간 100초가 적절한지는 사람이 플레이해 봐야 확정된다.
- **적 행동은 통상 공격뿐이다.** 적이 스킬과 상태이상을 쓰게 하면 전투의 깊이가 늘어난다.
